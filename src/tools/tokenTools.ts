import { JwtService } from '@nestjs/jwt';

import { signInDto } from 'src/auth/dto/sign-in.dto';

export class tokenTools{
    private static createToken(signInDto: signInDto){

        if (signInDto.email)

        return;
    }
}